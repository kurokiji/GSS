<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="230dp"
    android:layout_height="wrap_content"
    android:layout_marginVertical="10dp"
    android:layout_marginHorizontal="10dp">


    <TextView
        android:id="@+id/instructionsText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:textSize="30dp"
        android:textColor="@color/white"
        android:paddingVertical="10dp"
        app:layout_constraintBottom_toTopOf="@id/profilePic"
        android:textStyle="bold"
        android:text="Tap to update" />

    <ImageView
        android:id="@+id/profilePic"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:srcCompat="@drawable/welcome_shield" />

    <ImageView
        android:id="@+id/touchIcon"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"

        app:srcCompat="@drawable/welcome_hand" />

    <TextView
        android:id="@+id/dismissButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/touchIcon"
        app:layout_constraintStart_toStartOf="@id/touchIcon"
        app:layout_constraintEnd_toEndOf="@id/touchIcon"
        app:layout_constraintBottom_toBottomOf="parent"
        android:paddingTop="25dp"
        android:layout_marginBottom="20dp"
        android:text="Dismiss"
        android:textColor="@color/white"
        android:textSize="25dp"/>
</androidx.constraintlayout.widget.ConstraintLayout>